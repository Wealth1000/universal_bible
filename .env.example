# Supabase credentials used to fetch Rhapsody devotional content.
# Copy this file to `.env` and fill in the values.
# The publishable key is public by design (no auth); do NOT put a secret
# (service-role) key here.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
